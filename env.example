# Gmail MCP Server Environment Configuration

# Optional: Server Configuration
PORT=3010
SERVER_HOST=localhost

# Note: Google OAuth client credentials (GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET) 
# are configured in the MCP client, not on this server. 